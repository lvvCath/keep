<!-- RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301,NE]
Header always set Content-Security-Policy "upgrade-insecure-requests;"

# redirect 404 Error Pages
ErrorDocument 404 https://keepcs.000webhostapp.com/

# Prevent access to root directory
Options -Indexes 

# redirect 403 Error Pages
ErrorDocument 403 https://keepcs.000webhostapp.com/ -->